fmt := import("fmt")

export {
    config: {
        aspect_ratio: (3.0 / 2.0),
        image_width: 1000,
        samples: 500,
        depth: 50
    },

    scene: func(frame, seconds) {
        fmt.println("I'm a script!")
    }
}